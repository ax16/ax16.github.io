if (document.querySelectorAll('a#flag').length == 0){let flag = document.createElement('a');flag.setAttribute("id","flag");document.head.appendChild(flag);document.querySelector('script[src="HTTPS://AX16.GITHUB.IO/POC"]').closest('td').style="display: none";let im = document.querySelector('img.in-header');let x = im.clientWidth;im.src = "https://cataas.com/cat?width=" + x + "&height=" + x;const sleep = (time) => {return new Promise((resolve) => setTimeout(resolve, time))};const gatling = async () => {for (let i = x; i < x*5; i++) {await sleep(10);im.style.height = i+'px';im.style.width = i+'px';}}}
